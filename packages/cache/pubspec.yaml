name: t_cache
description: Efficiently store and retrieve data with TTL, LRU eviction.
version: 0.0.6
repository: https://github.com/tyrcord/t/tree/main/packages/cache

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  t_helpers: ^0.0.29

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
