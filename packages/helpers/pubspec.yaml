name: t_helpers
description: A collection of helper functions to simplify common programming tasks in Dart.
version: 0.0.38
repository: https://github.com/tyrcord/t/tree/main/packages/helpers

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  decimal: ^2.3.3
  intl: ^0.18.0
  rational: ^2.2.2
  diacritic: ^0.1.4

  # T
  tsub: ^1.7.0
  tmodel: ^0.5.11+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  import_sorter: ^4.6.0
