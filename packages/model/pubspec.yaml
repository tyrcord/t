name: tmodel
version: 0.5.8+1
description: A dart library that helps you to manage model (blueprint) objects.
homepage: https://github.com/tyrcord/t/tree/main/packages/tmodel
issue_tracker: https://github.com/tyrcord/t/tree/main/packages/issues

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  equatable: ^2.0.5

  # T
  t_helpers: ^0.0.29+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
