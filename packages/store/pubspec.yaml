name: tstore
description: A dart library that helps you to manage and store entity objects.
version: 0.8.8
repository: https://github.com/tyrcord/t/tree/main/packages/tstore
issue_tracker: https://github.com/tyrcord/t/tree/main/packages/tstore/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  rxdart: ^0.27.7
  meta: ^1.8.0
  equatable: ^2.0.5
  tmodel: ^0.5.3
  tsub: ^1.3.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  dart_code_metrics: ^5.7.2
